{
  "name": "katalis-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "generate": "nuxt build && nuxt generate"
  },
  "dependencies": {
    "nuxt": "^2.13.0"
  },
  "devDependencies": {}
}
